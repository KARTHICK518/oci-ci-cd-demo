version: 0.1

component: build

shell: bash

steps:
  - type: Command
    name: Build
    command: |
      echo "Hello OCI"
      mkdir output
      echo "sample" > output/test.txt

outputArtifacts:
  - name: website
    type: BINARY
    location: output
